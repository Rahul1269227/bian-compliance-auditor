[project]
name = "dummy-bian-compliance-auditor"
version = "0.1.0"
description = "Safe sample project for BIAN-style OpenAPI checks."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
